# Leave blank to use the Vite dev proxy (/api -> http://127.0.0.1:8000).
# Set this if your backend runs on a different host/port in production,
# e.g. VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
